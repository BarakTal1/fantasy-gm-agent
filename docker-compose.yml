services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fgm
      POSTGRES_PASSWORD: fgm
      POSTGRES_DB: fantasy_gm
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
